---
import { Image } from 'astro:assets';
import logo from '@assets/logo_knots.svg';
import Btn from '../ui/Btn.astro';
---

<div class="kv">
  <div class="inner">
    <hgroup class="hgroup">
      <h1 class="logo">
        <Image src={logo} alt="KNOTS" width="681" height="152" class="logo-img"/>
      </h1>
      <p class="catch">デザイン・AI・エンジニアリング、<br class="only-pc">先端ウェブ・プロダクトの潮流が "交わる" カンファレンス</p>
    </hgroup>
    <div class="meta">
      <span class="date font-en">
        <span class="date-body">2026.01.25</span>
        <span class="day-of-week">(SUN)</span>
      </span>
      <span class="place">@東京都中央区日本橋 日本橋タワー<span class="pc-space"></span><br class="only-sp">サイボウズ本社スペース</span>
    </div>
    <div class="btn-wrapper">
      <Btn href="#looking-for-sponsors" txt="スポンサー募集要項" modifier="kv"/>
    </div>
  </div>
</div>

<style>
  @layer components {
    .kv {
      color: var(--color_white);
      text-align: center;
    }
    .logo {
      width: 100%;
      max-width: 680px;
      overflow: hidden;
      margin: 0 auto 44px;
    }
    .logo-img {
      width: 100%;
      height: auto;
    }
    .catch {
      text-box: trim-both cap alphabetic;
      font-feature-settings: 'palt' on;
      font-size: rem(28);
      font-weight: 600;
      line-height: normal;
      letter-spacing: 0.04em;
      margin-bottom: 80px;
    }
    .meta {
      display: grid;
      text-box: trim-both cap alphabetic;
      row-gap: 20px;
    }
    .date {
      font-size: rem(28);
    }
    .date-body {
      font-size: rem(80);
      font-weight: 500;
    }
    .place {
      font-feature-settings: 'halt' on, 'palt' on;
      font-size: rem(20);
      font-weight: 600;
      letter-spacing: 0.02em;
    }
    .btn-wrapper {
      margin-block: 80px;
      border-top: 1px solid color-mix(in srgb, var(--color_white) 60%, transparent 40%);
      border-bottom: 1px solid color-mix(in srgb, var(--color_white) 60%, transparent 40%);
      padding-block: 40px;
    }
    @media (--sp) {
      .logo {
        width: vw(300);
        margin-bottom: 24px;
      }
      .catch {
        width: vw(300);
        margin-inline: auto;
        font-size: rem(16);
        margin-bottom: 56px;
      }
      .meta {
        row-gap: 12px;
      }
      .date {
        font-size: rem(16);
      }
      .date-body {
        font-size: rem(32);
      }
      .place {
        font-size: rem(14);
        line-height: 1.3;
      }
      .btn-wrapper {
        margin-block: 56px 80px;
        padding-block: 32px;
        padding-inline: 22px;
      }
    }
  }
</style>