---
import Layout from '@/layouts/Layout.astro'
import HeaderLower from '@/components/header/HeaderLower.astro'
import FooterLower from '@/components/footer/FooterLower.astro'
import LowerWrapper from '@/components/util/LowerWrapper.astro'
import SectionTitle from '@/components/util/SectionTitle.astro'
import InformationPanel from '@/components/archive/InformationPanel.astro'
import TrackFilter from '@/components/archive/TrackFilter.astro'
import VideoGrid from '@/components/archive/VideoGrid.astro'
import VideoCard from '@/components/archive/VideoCard.astro'
import videosData from '@/data/videos.json'
import type { Video } from '@/types/videoTypes'

const videos = (videosData as Video[]).filter((v) => v.published)
---

<Layout
  pageTitle="アーカイブ"
  description="KNOTSカンファレンスの登壇動画アーカイブです。Wave / Cross 2トラックのセッション動画をご覧いただけます。"
>
  <HeaderLower />
  <LowerWrapper>
    <div class="inner">
      <div class="archive">
        <SectionTitle
          en="Archive"
          ja="動画アーカイブ"
          heading_lv="h1"
        />
        <InformationPanel />
        <TrackFilter />
        <VideoGrid>
          {videos.map((video) => (
            <VideoCard video={video} />
          ))}
        </VideoGrid>
      </div>
    </div>
  </LowerWrapper>
  <FooterLower />
</Layout>

<style>
  @layer components {
    .archive {
      padding-bottom: 80px;
    }
    @media (--sp) {
      .archive {
        padding-bottom: 48px;
      }
    }
  }
</style>
